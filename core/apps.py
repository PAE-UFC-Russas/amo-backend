"""Configurações relacionadas ao aplicativo 'core'."""
from django.apps import AppConfig


class CoreConfig(AppConfig):
    """Configurações do aplicativo 'core'."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "core"
